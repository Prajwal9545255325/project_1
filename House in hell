<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home in Hell</title>
    <link href="https://fonts.googleapis.com/css2?family=Creepster&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to bottom, #1a1a1a, #3e0000);
            color: #fff;
        }

        .hero {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(to bottom, #3e0000, #1a0000);
            position: relative;
            overflow: hidden;
        }

        .hero h1 {
            font-size: 3rem;
            margin: 0 0 20px;
        }

        .hero p {
            font-size: 1.5rem;
            margin: 0 0 40px;
        }

        .hero button {
            background: #ff4500;
            border: none;
            padding: 15px 30px;
            color: #fff;
            font-size: 1.2rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .hero button:hover {
            background: #e63900;
        }

        .satan-container {
            position: relative;
            margin: 0 auto;
            width: 200px;
            height: 200px;
        }

        .satan {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 100px;
            background: url('https://via.placeholder.com/100x100?text=Satan') no-repeat center/cover;
            animation: bounce 2s infinite;
        }

        .cauldron {
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 150px;
            height: 80px;
            background: url('https://via.placeholder.com/150x80?text=Cauldron') no-repeat center/cover;
        }

        @keyframes bounce {
            0%, 100% {
                transform: translate(-50%, 0);
            }
            50% {
                transform: translate(-50%, -20px);
            }
        }
    </style>
</head>
<body>
    <section class="hero">
        <h1>Welcome to Home in Hell</h1>
        <p>Reserve your exclusive plot in the underworld. Eternal torment never looked this good!</p>
        <div class="satan-container">
            <div class="satan"></div>
            <div class="cauldron"></div>
        </div>
        <button onclick="alert('Explore more plots in Hell!')">Explore More</button>
    </section>
</body>
</html>
